# HS-AD3 — "signature valid": one real job becoming a signed public record

A 9-second 1080x1920 hero insert, built with the `motion-graphics` skill
(reborn-motion-skills, installed 2026-09-04) under its hard gate: research →
approved-reference preflight → build authorization → candidate evidence →
independent critic → delivery authorization. No stage may be skipped and the
builder cannot grade its own work.

## The one idea

The agent that does the work never grades it, and the outcome ships as a
signature anyone can check. Nothing else is claimed.

## Posture: SHIPPED — and every value is a real one

Built entirely on **job #30** on the owner's live mainnet account, read
2026-09-04 and cross-checked on three surfaces before use (`my_work`,
`get_job`, `get_work_proof`) because the DO NOT CLAIM ledger records that a
single-surface verdict has been wrong before (I-01, L-08):

| Shown | Value | Source |
|---|---|---|
| job | #30 "Final answer — verified findings only" | `get_job 30` |
| worker | Editor (`l6F52Ds58…`), on ENG.FLOOR | `get_job 30`, `list_my_agents` |
| grading | independent grader, verdict PASSED | `get_job 30` |
| fingerprint | keccak256 `0x5b44eef1…a37af6df` | `get_work_proof 30` |
| oracle | `0x81C76907…7a3D3B68`, signature VALID | `get_work_proof 30` |
| bounty | $1, Completed (done and paid) | `get_job 30` |
| certificate | /proof/c18db650-1312-4027-9a57-5fa515f9b2e7 | live public page |

Not claimed anywhere in the cut: traction, audit, security, full autonomy,
or any Verification Lawbook code.

## Both surfaces are real captures, not recreations

- **The office** — the handsel repo's own `TacticalView` renderer (shipped to
  main 2026-09-04) run against this account's **real roster**: Editor,
  Independent Check, Red Team, Fact Checker, US Trading Desk at their desks.
- **The certificate** — the live public page at handsel-main.vercel.app,
  captured at 1200x1400 @2x. Its Korean type is the product's own; the clip
  adds no Korean text of its own.

## Mechanism and motion

Approved reference: `smartlead-prewarmed-burn` — authentic product UI supplies
identity, the camera travels along a literal causal connector, and a verified
system state resolving is the payoff. Secondary grammar from
`kickbacks-same-run-editor`: the product UI is the world, and the payoff stays
attached to the working context.

hold (office, JOB #30) → the deliverable's keccak256 hash leaves the desk and
rides the wire → hold (the certificate that hash became; its own fingerprint,
signature-verified and verdict rows bracket in sequence) → pull-back hold
(BOUNTY $1 USDC · PAID, and the certificate URL, held still long enough to be
read and opened by hand).

Ground is neutral charcoal — flat, unlit. No neon, glow, bloom, gradient wash
or glassmorphism, per the skill's hard rules.

## Status

DRAFT, and **not gate-authorized**. Five versions were built and five
independent critic reviews run; none reached the gate's bar (every axis ≥8,
average ≥8.5). Authenticity scored 9/9/9 in the final review and no invented
value was ever found; motion and readability never cleared, for reasons that
are properties of the material rather than the edit — see `qc.md`.

Publishing is NOT authorized either way — Charter rule: nothing publishes
without explicit human approval, every time.
