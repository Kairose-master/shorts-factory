{
  "name": "handsel-instagram-integration",
  "private": true,
  "version": "0.1.0",
  "description": "Staged Instagram publishing integration for the Handsel repo (see PORTING.md)",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "gates": "npm run typecheck && npm run test"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
